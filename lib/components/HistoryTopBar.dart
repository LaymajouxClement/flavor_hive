import 'package:flutter/cupertino.dart';

class HistoryTopBar extends StatelessWidget {
  const HistoryTopBar({Key? key}) : super(key: key);


  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
